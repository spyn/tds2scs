{
  "helixModule": "Module name", // This is the helix module name, eg /Feature/<ModuleName>/
  "maxRelativePathLength": 100, // ommit if you want to use default
  "solutionFile": "Path to solution file", // Can specify here, or on the command line
  "serialisationFolder": "Seralisation folder" // ommit if you want default
}